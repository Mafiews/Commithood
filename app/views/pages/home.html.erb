
<!-- yizhu: banner part -->
<div class="banner-hood" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('https://images.unsplash.com/photo-1525026198548-4baa812f1183?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1533&q=80')">

  <div class="search-bar">
  <%= form_tag events_path, method: "get" do %>
    <%= text_field_tag :address, "", required: false, id: "event-address", label: false, placeholder: "Type your address" %>
    <%= select_tag 'user-cause', options_for_select(Cause::CAUSES), class: 'form-control event-address mt-3' %>
    <%= submit_tag "Go", class: "btn btn-success text-white ml-2 mt-3" %>
  <% end %>
  </div>
</div>


<!-- yizhu create 3 cards to show the lastest 3 events registed under the banner, in form of card -->
<div class="container">
  <h1>Popular events is happing now</h1>
  <div class="row justify-content-rounded">
    <% @events.each do |event| %>
      <div class="card" style="width: 18rem;">
        <img src="..." class="card-img-top" alt="event image">
        <div class="card-body">
          <h5 class="card-title"><%= event.name %></h5>
          <p class="card-text"><%= event.description %></p>
          <%= link_to "Paticiper", event_path(event), class:"btn btn-success" %>
        </div>
      </div>
    <% end %>
    </div>
  </div>

