<div class="login">
  <div class="container col-10 pt-5" id="login-container">
    <h2 class="text-center mb-5" id="subscription">CONNEXION</h2>
      <div class="row-justify-content-center">
        <div class="col-12 ">

          <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <div class="form-inputs">
                  <%= f.input :email,
                              required: false,
                              autofocus: true,
                              input_html: { autocomplete: "email" } %>
                  <%= f.input :password,
                              required: false,
                              input_html: { autocomplete: "mot de passe" }, label:"Mot de passe" %>
                  <%= f.input :remember_me, as: :boolean, label: "Rester connecté(e)" if devise_mapping.rememberable? %>
                    <div class="form-actions">
                      <%= f.button :submit, "C'est parti ! 💪", class: "btn btn-success btn-lg w-100"%>
                    </div>
                  <% end %>
                    <%= render "devise/shared/links" %>
              </div>
            </div>
          </div>
        </div>
      </div>





